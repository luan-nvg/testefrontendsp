
import {
    StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        justifyContent: 'flex-start',
        marginTop: '5%'
    },
    Text: {
        color: '#ffff',
        fontSize: 20
    },
    opacity: {
        alignItems: 'center',
        backgroundColor: '#FF8686',
        padding: '5%',
        borderRadius: 15,
    }
});

